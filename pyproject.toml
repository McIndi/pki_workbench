[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pki-workbench"
version = "0.1.0"
description = "PKI Workbench"
requires-python = ">=3.11"

[project.scripts]
pki = "pki_cli.main:main"
